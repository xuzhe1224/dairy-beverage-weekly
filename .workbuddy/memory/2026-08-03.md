# 2026-08-03 工作日志

## Codex 迁移方案简化

许哲嫌第一版迁移方案"太杂"（7 个文件），要求精简到"扔一个东西给 Codex 就能完成"。

### 重新交付 `codex-migration/` 目录（3 个文件版本）

| 文件 | 作用 |
|---|---|
| `codex-task.md` | 核心任务文件，Codex 读这个就懂"做什么、怎么做"（6 大步骤、含完整路径/token/风格规范） |
| `deploy.sh` | 部署脚本，路径 bug 已修复（从 codex-migration/deploy.sh 出发自动定位 eo-deploy 目录） |
| `README.md` | 一句话用法 + 三个关键提醒 |

### 一句话用法

```bash
codex exec --approval-mode full-auto "$(cat codex-task.md)"
```

Codex 会自己完成：环境检查 → 验证部署链路 → 写 run-report.sh → 配置定时调度 → 跑一次测试 → 给用户回传 URL。

### 删除的冗余文件

- AGENTS.md（合并到 codex-task.md）
- config.toml（Codex 默认即可，web_search 启用在 task 里说明）
- com.dairy-beverage-weekly.plist（让 Codex 自己决定 launchd vs crontab）
- run-weekly.sh（让 Codex 自己写 run-report.sh）
- MIGRATION-GUIDE.md（精简成 README.md）
- memory.md（历史执行记录已留在 WorkBuddy 这边）

### deploy.sh 路径 bug 修复

旧版 `deploy.sh` 假设 eo-deploy 在 deploy.sh 同级目录的 eo-deploy/ 子目录，实际应该在 codex-migration 的父目录。
新逻辑：`SCRIPT_DIR/deploy.sh` → `SCRIPT_DIR/..` 即项目根 → `项目根/eo-deploy/`，并支持 `DEPLOY_DIR` 环境变量覆盖。
